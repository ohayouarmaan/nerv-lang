struct Point { x: int, y: int }

extern printf(string, int) int;

@add(int a, int b) int {
  return a + b;
}

@main() int {
  dec p Point = #Point { x: 3, y: 4 };
  dec sumFn fn(int, int) -> int = add;
  dec result int = sumFn(p.x, p.y);
  printf("sum: %d\n", result);
  return result;
}
