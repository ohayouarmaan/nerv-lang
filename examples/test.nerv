extern printf(string, &int) int;
extern malloc(int) &int;

@main() int {
  dec v &int = malloc(4);
  printf("Pointer for int: %p", v);
  v = malloc(14);
  printf("Pointer for int: %p", v);
  return 0;
}
