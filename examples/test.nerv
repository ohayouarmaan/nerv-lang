extern printf(string, int) int;
extern malloc(int) &int;

@main() int {
  dec v int = 9;
  dec x &int = &v;
  printPointer(x, 2);
  return 0;
}

@printPointer(&int x, int wow) void {
  printf("Pointer %d", *x);
}
